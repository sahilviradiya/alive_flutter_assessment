/// Named route identifiers, kept separate from the route generator so
/// screens can reference them without importing navigation logic.
class AppRoutes {
  AppRoutes._();

  static const String splash = '/splash';
  static const String login = '/login';
  static const String home = '/home';
}
